[project]
name = "neural-ledger"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "duckdb>=1.5.2",
    "pytest>=9.0.3",
    "python-dotenv>=1.2.2",
    "watchdog>=6.0.0",
]
